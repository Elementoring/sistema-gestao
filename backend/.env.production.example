# Exemplo de configuração de variáveis de ambiente para produção
# NÃO COMMITE ESTE ARQUIVO COM VALORES REAIS!

# Configuração do Servidor
PORT=3001
NODE_ENV=production

# Database - Será fornecido automaticamente pelo Render.com
DATABASE_URL=postgresql://usuario:senha@host:5432/database

# JWT Secret - GERE UMA CHAVE FORTE!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_secreta_super_forte_aqui

# Credenciais do Admin (usado pelo script setup-db.ts)
ADMIN_PASSWORD=SenhaForte123!@#
ADMIN_USERNAME=admin
ADMIN_FULL_NAME=Administrador

# CORS - URLs permitidas (separadas por vírgula)
# Exemplo: http://localhost:5173,https://seu-frontend.onrender.com
ALLOWED_ORIGINS=https://cred-management-frontend.onrender.com,http://localhost:5173
ALLOWED_ORIGINS=https://seu-frontend.onrender.com,https://seu-dominio.com

# Upload de Arquivos
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
